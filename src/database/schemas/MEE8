const mongoose = require('mongoose')

const mee8 = mongoose.Schema({

    guildId: {type: String},
    channel: {type: String},
    message: {type: Array, default: []},
    tag: {type: Array, default: []},
    image: {type: Array, default: []},
   
})

module.exports = mongoose.model('mee8', mee8)